# Git config format 

```bash
git config --global user.name "tu nombre"

git config --global user.email "tu@email.com"
```

## Para que sirve?

Sirven para establecer el nombre que aparece en cada commit realizado y para vincular los cambios que haces en el repositorio de tu cuenta en git



